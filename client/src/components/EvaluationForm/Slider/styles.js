import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
    box: {
        padding: '0px',
        marginTop: '16px',
        marginBottom: '8px',
    },
    title: {
        marginBottom: '8px',
    },
    rating: {
        display: 'flex',
        alignItems: 'center',
    },
    points: {
        fontSize: '22px',
    }
});

export default styles;